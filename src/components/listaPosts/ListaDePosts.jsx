/* import Post from "./Post";

const Postlist = ({ posts }) => {
  return posts.length ? (
    <ul>
      {posts.map((post) => (
        <li key={post.postId}>
          <Post post={post} />
        </li>
      ))}
    </ul>
  ) : (
    <p>No hay posts todavía</p>
  );
};
export default Postlist;
 */
